# Monthly Subs Balance Breakdown

Add a monthly breakdown view to the Subs section of PlayDay.

## Tasks
- [x] Add helper utilities for parsing dates into month keys and labels
- [x] Compute per-month aggregates (matches, expected, paid, outstanding, owing players) using existing match history, subs payments, and price history
- [x] Compute per-player per-month breakdown (games played, expected, paid, balance)
- [x] Add a "Months" tab to the main Subs screen with a scrollable list of month cards (most recent first), tap-to-expand showing players who owe money
- [x] Add a monthly breakdown section to the player modal with running balance at the top and per-month rows
- [x] Use the existing colour scheme (green = paid, red = owing, amber = partial)
